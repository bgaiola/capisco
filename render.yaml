services:
  - type: web
    name: capisco
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm run build:all
    startCommand: npm run start:prod
    envVars:
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
